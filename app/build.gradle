apply plugin: 'com.android.library'

android {
    compileSdkVersion 32
    defaultConfig {
        //applicationId "com.necatitufan.tufancircleseekbar"
        minSdkVersion 18
        targetSdkVersion 32
        versionCode 1
        versionName "1.0"
        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
        }
    }